# EazyFill Firefox Extension

This folder is the Firefox-specific extension source.

- `manifest.json` is the Firefox/Gecko manifest and includes `browser_specific_settings.gecko`.
- Shared runtime code mirrors the main `extension/` folder.
- Backend XPI packaging uses this folder directly.
