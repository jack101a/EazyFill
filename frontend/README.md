# EazyFill Admin Console

React operations console served by the EazyFill FastAPI backend.

## Run Locally

```powershell
cd frontend
npm install
npm run dev
```

## Build

```powershell
cd frontend
npm run build
```

FastAPI serves the built files from `frontend/dist/` under `/admin`.
